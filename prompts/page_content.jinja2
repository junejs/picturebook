请根据以下信息，生成完整的儿童绘本分页内容。

书名：{{ title }}
故事创意：{{ idea }}
语言：{{ language }}
目标年龄：{{ target_age }}
艺术风格：{{ style }}
页数：{{ page_count }}

{% if characters %}
# 角色身份（预定义）

以下角色的外观已在所有页面中固定。请使用这些描述来确保故事和插图中角色的一致性。

{{ characters }}

**重要**: 插图提示中不要重复这些角色的完整外观描述，只描述本页特有的场景、动作和临时变化。
{% endif %}

要求：
1. 生成严格等于 {{ page_count }} 页的内容。
2. 故事必须具有完整叙事弧线：
   - 开头：介绍角色和场景
   - 中段：推进冒险、冲突或探索
   - 结尾：解决问题并收束情绪
3. 每页 `story_text`：
   - 使用 {{ language }}
   - 1 到 3 句
   - 适合儿童阅读
   - 每页都要推动故事
4. 每页 `illustration_prompt`：
   - 使用中文自然语言描述，用简洁连贯的句子写明主体、动作、环境。
   - 2 到 4 句，以"{{ style }}风格绘本插画"开头。
   - 明确描述画面中的角色行为和所处环境。
   - 远景构图，角色在画面中比例较小，重点展示环境氛围。
   - 不要堆叠英文关键词，不要描述面部特写或局部细节。
   - 不要在画面中添加文字、水印或 logo。
   - 严禁重复角色的永久外貌描述（服装颜色、发型、体型等），只描述本页的临时变化。
{% if characters %}
   - 提示词会自动拼接角色参考图信息，你只需要描述场景和角色动作。
{% endif %}
5. 只返回 JSON，格式如下：

{
  "pages": [
    {
      "page_number": 1,
      "story_text": "...",
      "illustration_prompt": "..."
    }
  ]
}
