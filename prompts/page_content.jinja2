请根据以下信息，生成完整的儿童绘本分页内容。

书名：{{ title }}
故事创意：{{ idea }}
语言：{{ language }}
目标年龄：{{ target_age }}
艺术风格：{{ style }}
页数：{{ page_count }}

要求：
1. 生成严格等于 {{ page_count }} 页的内容。
2. 故事必须具有完整叙事弧线：
   - 开头：介绍角色和场景
   - 中段：推进冒险、冲突或探索
   - 结尾：解决问题并收束情绪
3. 每页 `story_text`：
   - 使用 {{ language }}
   - 1 到 3 句
   - 适合儿童阅读
   - 每页都要推动故事
4. 每页 `illustration_prompt`：
   - 使用英文
   - 2 到 4 句
   - 描述该页特有的场景、动作、情绪、构图、镜头、环境
   - 明确体现 {{ style }}
   - 不要包含图片中的文字、水印或 logo
5. 只返回 JSON，格式如下：

{
  "pages": [
    {
      "page_number": 1,
      "story_text": "...",
      "illustration_prompt": "..."
    }
  ]
}
