请根据以下信息，生成完整的儿童绘本分页内容。

书名：{{ title }}
故事创意：{{ idea }}
语言：{{ language }}
目标年龄：{{ target_age }}
艺术风格：{{ style }}
页数：{{ page_count }}

{% if characters %}
# 角色身份（预定义）

以下角色的外观已在所有页面中固定。请使用这些描述来确保故事和插图中角色的一致性。

{{ characters }}

**重要**: 插图提示中不要重复这些角色的完整外观描述，只描述本页特有的场景、动作和临时变化。
{% endif %}

要求：
1. 生成严格等于 {{ page_count }} 页的内容。
2. 故事必须具有完整叙事弧线：
   - 开头：介绍角色和场景
   - 中段：推进冒险、冲突或探索
   - 结尾：解决问题并收束情绪
3. 每页 `story_text`：
   - 使用 {{ language }}
   - 1 到 3 句
   - 适合儿童阅读
   - 每页都要推动故事
4. 每页 `illustration_prompt`：
   - 使用英文
   - 2 到 4 句
   - 每页必须以 "wide shot" 或 "long shot" 开头，远景构图展示角色与环境的整体关系
   - 描述该页特有的场景、动作、情绪、环境
   - 明确体现 {{ style }}
   - 不要包含图片中的文字、水印或 logo
{% if characters %}
   - 不要重复角色的永久外貌描述（服装颜色、发型、体型等），只描述本页的临时变化
{% endif %}
5. 只返回 JSON，格式如下：

{
  "pages": [
    {
      "page_number": 1,
      "story_text": "...",
      "illustration_prompt": "..."
    }
  ]
}
